{
    "name": "l1t-webhook-node",
    "version": "1.0.0",
    "description": "L1 Automated Triage webhook Lambda: routes CloudWatch alarm events to the AWS DevOps Agent via an HMAC-signed webhook, with dedup and Slack fallback",
    "main": "index.js",
    "scripts": {
        "test": "jest",
        "lint": "eslint *.js"
    },
    "author": "",
    "license": "Apache-2.0",
    "dependencies": {
        "@aws-sdk/client-dynamodb": "^3.700.0",
        "@aws-sdk/lib-dynamodb": "^3.700.0",
        "@aws-sdk/client-secrets-manager": "^3.700.0"
    },
    "engines": {
        "node": ">=22.0.0"
    }
}
